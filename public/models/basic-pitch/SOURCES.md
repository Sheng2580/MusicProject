# Basic Pitch model

`model.json` and `group1-shard1of1.bin` are the TensorFlow.js model shipped
with [`@spotify/basic-pitch` 1.0.1](https://github.com/spotify/basic-pitch-ts).
The package and model are distributed under the Apache License 2.0; the full
license text is included in `LICENSE` in this directory.

The files are copied without modification from
`node_modules/@spotify/basic-pitch/model/`.
